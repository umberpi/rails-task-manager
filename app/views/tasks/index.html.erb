<h1>My Tasks</h1>

<div class="boxes_tasks">
  <ul>
    <% @tasks.each do |task| %>
      <li>
        <%= task.completed ? "✅" : "☠️" %>
        <%= link_to task.title, task_path(task)%>
        <%= link_to "Delete", task_path(task), method: :delete  %>
      </li>
    <% end %>
  </ul>
</div>
